.clearfix
	cf()

.stackable
	& > *
		stack()

.stack
	stack()

.unstack
	unstack()
	
.relative
	position relative

.fixed
	position fixed

.absolute, .pin
	position absolute

.pin, .fixed
	&.top
		top 0
	&.left
		left 0
	&.right
		right 0
	&.bottom
		bottom 0
	&.fixed
		position fixed

.hidden
	display none !important

.invisible
	visibility hidden !important

.centered
	&:not(.vertical):not(.horizontal)
		align(both)
	&.vertical
		align(vertical)
	&.horizontal
		align(horizontal)

.nowrap
	white-space nowrap
